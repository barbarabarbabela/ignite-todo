import { PlusCircle } from "phosphor-react";
import { ButtonStyle } from "./Button.styled";

export const Button = () => {
  return (
    <ButtonStyle>
      Criar <PlusCircle size={20} />{" "}
    </ButtonStyle>
  );
};
