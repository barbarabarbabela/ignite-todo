import styled from "styled-components";

export const FormContainer = styled.form`
  display: flex;
  gap: 0.5rem;

  position: absolute;
  top: 10.75rem;
  left: 50%;
  transform: translateX(-50%);

  height: 3.375rem;
`;
